"""
Droid-FineTuning v2 backend package.

Rebuilt on top of the `mlx-lm-lora` training engine (pip package).
See backend/app/main.py for the FastAPI application that serves the
existing React frontend contract.
"""
